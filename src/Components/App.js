var React = require('react');
var PlanetWrapper = require('./PlanetWrapper')
var App = React.createClass({
  render: function() {
    return (
      <PlanetWrapper />
    );
  }
})

module.exports = App;
